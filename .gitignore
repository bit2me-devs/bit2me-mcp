# Node.js & TypeScript project
node_modules/
.pnpm-store/

# Build output
/dist/
/build/

# TypeScript cache
*.tsbuildinfo

# Environment files
.env
.env.*
!.env.example
.env.local
.env.*.local
.envrc

# Logs
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# OS generated files
.DS_Store
Thumbs.db

# Editor directories
.vscode/
.idea/
*.sublime-workspace
*.sublime-project
*.iml

# Cursor / Claude — rules y skills son locales, no se versionan
.cursor/
.claude/

# Código / binarios de feat/go-migration. El árbol `go/` vive en esa rama;
# en main solo quedarían artefactos sueltos (p. ej. go/bin/) al cambiar de rama.
/go/

# Test coverage
coverage/

# Test credentials (contains secrets)
test-credentials.js

# Miscellaneous
*.tgz
*.zip
*.pem
*.key

# Temporary files
tmp/
*.tmp

# Local-only documentation (map, ADRs, playbooks, initiative diary)
docs/

# Assets locales (imágenes temporales)
assets/

# Landing catalogue / LLM dumps — generated in Pages (build:docs / build:llms)
landing/tools-data.js
landing/llms.txt
landing/llms-full.txt
