name: Deploy Landing Page

on:
    # Push to main keeps the landing in sync with non-release changes
    # (HTML tweaks, copy edits, llms.txt regeneration, etc.).
    push:
        branches: ["main"]

    # `release: published` re-runs the deploy *after* semantic-release
    # cuts a new GitHub Release. Without this hook the push-triggered
    # deploy and the release run race, so the landing ends up
    # advertising the previous tag for one cycle. Triggering on
    # `published` instead of `created` ensures the tag is fully visible
    # to `git describe` by the time the checkout runs.
    release:
        types: [published]

    workflow_dispatch:

# Permisos necesarios para que el bot pueda escribir en GitHub Pages
permissions:
    contents: read
    pages: write
    id-token: write

# Solo permitimos un deploy a la vez para no liar las cosas
concurrency:
    group: "pages"
    cancel-in-progress: false

jobs:
    deploy:
        environment:
            name: github-pages
            url: ${{ steps.deployment.outputs.page_url }}
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
              with:
                  # `generate-tools-docs.js` resolves the landing-page
                  # version from `git describe --tags --abbrev=0`. The
                  # default shallow checkout (`fetch-depth: 1`) doesn't
                  # bring tags, so we either ask for tags explicitly or
                  # do a full fetch. We pick `fetch-tags: true` because
                  # it's the smallest extra payload that still gives us
                  # the data the script needs.
                  fetch-depth: 0
                  fetch-tags: true

            - name: Setup pnpm
              uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6

            - name: Setup Node
              uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
              with:
                  node-version: "20"
                  cache: "pnpm"

            - name: Install dependencies
              run: pnpm install --frozen-lockfile

            - name: Lint
              run: pnpm run lint

            - name: Test
              run: pnpm test

            - name: Build
              run: pnpm run build

            - name: Generate landing tools data (embeds latest released version)
              run: pnpm run build:docs

            - name: Generate LLM Docs
              run: pnpm run build:llms

            - name: Minify HTML for production
              run: MINIFY=true node scripts/minify-html.js

            - name: Configurar GitHub Pages
              uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0

            - name: Subir los archivos de la carpeta docs
              uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
              with:
                  path: "./landing"

            - name: Desplegar a GitHub Pages
              id: deployment
              uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
